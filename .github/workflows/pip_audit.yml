name: PIP Audit

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  pip_audit:
    uses: OpenVoiceOS/gh-automations/.github/workflows/pip-audit.yml@fa39846b074ac7a856c8af5f8c0dec12b1fff1aa
    secrets: inherit
